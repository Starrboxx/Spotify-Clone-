import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spotify_Clone',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
